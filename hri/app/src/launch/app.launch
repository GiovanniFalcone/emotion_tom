<launch>

    <arg name="id" /> 
    <param name="id" value="$(arg id)"/>

	<arg name="condition" /> 
    <param name="condition" value="$(arg condition)"/>

    <!-- Flask node: run server -->
    <node name="flask_node" pkg="app" type="app.py" output="screen"/>
</launch>
